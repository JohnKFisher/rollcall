# Redesign Rationale

Historical UX decision record for the completed Roll Call redesign.

These documents explain why the current UX direction exists. They are not standalone authorization to rewrite the app. For current living UX guidance, use `product/UX_RULEBOOK.md`.

Current implementation wins when it differs from these documents, but meaningful drift should be surfaced rather than silently "fixed."
